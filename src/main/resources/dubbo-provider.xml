<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd

    http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
	<description>cboard</description>
	<!-- application anme -->
	<dubbo:application name="cboard" />
	<dubbo:provider retries="0" timeout="${dubbo.timeout}" />
	<dubbo:registry protocol="zookeeper" address="${zookeeper.addr}" />
	<dubbo:reference id="functionDubboService" interface="com.yyjz.icop.support.api.service.FunctionDubboService" check="false" url="${dubbo.url.functionDubboService}" filter="logcontext" timeout="10000" />
</beans>